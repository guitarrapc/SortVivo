# SortVivo カード並び替え機能 仕様書

## 概要

SortCardGrid コンポーネントにドラッグ&ドロップによるソートカード並び替え機能を追加する。  
JavaScript で実装し、WASM との通信オーバーヘッドを最小化してスムーズな UX を提供する。

---

## 1. 目的・要件

### 1.1 主要目的
- ユーザーが複数の比較カードの表示順序を自由に変更できるようにする
- デスクトップ（グリッド表示）でのドラッグ操作を実装
- モバイル（カルーセル表示）では別の並び替え手段を検討（後述）

### 1.2 非機能要件
- **パフォーマンス**: 60fps を維持し、ドラッグ中のラグを最小化
- **レスポンシブ**: グリッドレイアウト（2列/3列）に対応
- **永続化**: 現在の URL 状態管理と同様に、並び替え順序を `cards` クエリパラメータに反映
- **アクセシビリティ**: 視覚的フィードバックを明確に、将来的なキーボード操作を考慮した設計

---

## 2. 動作仕様

### 2.1 ドラッグ操作（デスクトップ）

#### 2.1.1 トリガー条件
- **長押し検出**: ポインターダウンから **300ms** 経過で ドラッグモード開始
- **対象デバイス**: マウス、タッチスクリーン（Pointer Events API で統一）
- **対象要素**: `.sort-card` コンテナ全体（`.sort-card__header` など部分要素でも可）

#### 2.1.2 長押し検出中（0〜300ms）
- **視覚フィードバック**: なし（通常のホバー状態のみ）
- **動作**: タップやクリックとの誤認を防ぐため、長押し確定まで待機

#### 2.1.3 ドラッグ開始（300ms 経過時）
- **カードの状態変化**:
  - ドラッグ中のカードに `.sort-card--dragging` クラスを追加
  - `opacity: 0.5` に設定（元の位置を視認可能に）
  - `z-index: 1000` で他の要素より前面に配置
- **ドラッグプレビュー**:
  - カードのクローンを作成し、ポインターに追従する DOM 要素として表示
  - プレビューには `.sort-card--drag-preview` クラスを付与
  - `position: fixed` で viewport 基準の絶対配置
  - `transform: translate3d(x, y, 0)` で GPU アクセラレーションを活用（サイズは等倍）
  - プレビューは `pointer-events: none` で他の要素との干渉を防ぐ
- **その他の準備**:
  - カルーセルのスクロールを無効化（`carouselInterop.disableScroll(gridId)`）
  - ブラウザのデフォルト動作（テキスト選択など）を `preventDefault()` で抑制

#### 2.1.4 ドラッグ中（pointermove）
- **プレビュー追従**: ポインター座標に合わせて `translate3d` を更新
- **並び替えシミュレート**:
  - プレビューの中心座標が他のカードの境界（50% 以上）を超えた場合、挿入位置を計算
  - 他のカードを `transform: translateX/Y` でアニメーション移動（`transition: transform 200ms ease-out`）
  - 挿入位置インジケーター（`.drop-indicator`）を表示（オプション）
- **スクロール境界検出**:
  - グリッドコンテナの上下端から **50px** 以内にプレビューが到達した場合、自動スクロールを開始
  - スクロール速度: 5px/frame（requestAnimationFrame ベース）

#### 2.1.5 ドロップ時（pointerup）
- **新しい順序の確定**:
  - 現在のシミュレート位置を最終的な挿入位置とする
  - 元の位置と同じ場合は何もしない（No-op）
- **アニメーション**:
  - ドラッグプレビューをフェードアウト（`opacity: 0`, 100ms）
  - ドラッグ中カードを新しい位置へ `transform` でスムーズに移動（200ms）
  - アニメーション完了後、CSS クラスをリセットし DOM を再構築
- **C# への通知**:
  - `dotNetRef.invokeMethodAsync('OnReorder', newOrderArray)` で新しい順序配列を送信
  - `newOrderArray` は `[0, 2, 1, 3]` のような元のインデックスの配列
- **クリーンアップ**:
  - カルーセルスクロールを再有効化
  - イベントリスナーをクリア

#### 2.1.6 キャンセル条件
- **Escキー押下**: ドラッグを中止し、元の状態に復元
- **ポインターがビューポート外**: ドロップ扱いで現在位置を確定

### 2.2 モバイル対応（カルーセルモード）

#### 2.2.1 現状の課題
- カルーセルは横スクロールが主要操作であり、ドラッグ操作と干渉する
- 長押しでのドラッグは誤操作を招きやすい（スクロール意図との区別が困難）

#### 2.2.2 代替案（検討中・実装は保留）
以下のいずれかを将来的に実装する可能性がある：

**案1: 並び替えボタン UI**
- 各カードに「並び替えモード」トグルボタンを追加
- トグル ON 時、カード下部に「← →」移動ボタンを表示
- ボタンタップで隣接カードと入れ替え

**案2: 編集モードへの切り替え**
- QuickAccessPanel または PlayControlBar に「編集モード」ボタン追加
- 編集モード時はカルーセルスクロールを無効化し、ドラッグを有効化

**案3: ドラッグハンドル**
- カードの一部（例: ヘッダー左端のアイコン）を「ドラッグハンドル」とする
- ハンドル上の長押しのみドラッグを開始（他の領域は通常のスクロール）

**決定**: 現時点では実装せず、デスクトップ版の使用感を評価してから判断

---

## 3. 永続化仕様

### 3.1 URL クエリパラメータ
- **現状**: `?cards=Quicksort|MergeSort|HeapSort` の形式で順序を保持
- **並び替え後**: カードの新しい順序を `cards` パラメータに反映
- **実装**: `SortCardGrid.OnReorder` → `Index.UpdateContentStateUrl()` の既存フローを活用

### 3.2 localStorage
- **対象外**: カード順序は保存しない（URL が単一の真実の情報源）
- 理由: ユーザーが URL を共有した際に順序も再現されるべきため

---

## 4. アニメーション仕様

### 4.1 固定パラメータ
| 項目 | 値 | 備考 |
|------|-----|------|
| 長押し検出時間 | 300ms | 一般的な UX ガイドライン準拠 |
| カード移動アニメーション | 200ms | ease-out カーブ |
| プレビューフェードアウト | 100ms | linear |
| 自動スクロール速度 | 5px/frame | 約 300px/秒（60fps 想定） |


---

## 5. アクセシビリティ

### 5.1 現時点の対応
- **視覚フィードバック**: ドラッグ中の透明度変化、プレビューの影表示、元の位置の点線枠
- **ARIA 属性**: 将来的に `aria-grabbed="true"`, `aria-dropeffect="move"` を検討

### 6.2 将来の拡張（キーボード操作）
- `Ctrl + ↑/↓` で選択中のカードを上下に移動
- `Tab` でカードにフォーカス、`Space` で選択状態切り替え
- スクリーンリーダーでの並び替え完了通知（`role="status"` の live region）

---

## 6. テスト計画

### 6.1 手動テスト項目
- [ ] デスクトップ Chrome/Edge/Firefox でドラッグ操作が正常動作
- [ ] モバイル Safari/Chrome でカルーセルスクロールと干渉しない
- [ ] 2列/3列グリッドでの並び替えが正しく動作
- [ ] 長押し300ms 未満でドラッグがキャンセルされる
- [ ] Esc キーでドラッグがキャンセルされる
- [ ] 並び替え後 URL の `cards` パラメータが更新される
- [ ] URL をコピーして新しいタブで開いた際、並び替え順序が再現される

### 6.2 パフォーマンステスト
- [ ] 6枚のカードを同時にドラッグしても 60fps を維持
- [ ] ドラッグ中の CPU 使用率が 30% 以下（DevTools Performance）

---

## 7. 未決定事項・今後の検討

### 7.1 モバイル対応の方針
- 現時点では実装保留
- デスクトップ版のフィードバックを得てから、3つの案から選択

### 7.2 カード数制限
- 現在の `ComparisonState.MaxComparisons = 6` を維持
- 6枚以上でのドラッグ操作は性能検証が必要

### 7.3 アニメーションのカスタマイズ
- 固定値で実装
- ユーザーからの要望があれば Settings に追加を検討

---

## 8. 実装スケジュール（参考）

1. **Phase 1**: JavaScript ドラッグロジックの実装（dragInterop.js）
2. **Phase 2**: C# との統合（SortCardGrid.razor）
3. **Phase 3**: CSS アニメーションの調整
4. **Phase 4**: テスト＆デバッグ
5. **Phase 5**: パフォーマンス最適化
6. **Phase 6**: モバイル対応の検討（フィードバック後）

---

## 10. 参考リンク

- [Pointer Events API - MDN](https://developer.mozilla.org/en-US/docs/Web/API/Pointer_events)
- [ドラッグ＆ドロップ UX ベストプラクティス](https://www.nngroup.com/articles/drag-drop/)
- [CSS GPU アクセラレーション](https://www.paulirish.com/2012/why-moving-elements-with-translate-is-better-than-posabs-topleft/)

